export class Technology {
    public id: number;
    public name: string;
    public m_code: string;

    constructor(name: string, m_code: string) {
        this.name = name;
        this.m_code = m_code;
    }
}
